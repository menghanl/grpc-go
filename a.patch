--- transport.go	2022-01-28 12:05:20.228857610 -0800
+++ transport.go.2	2022-01-28 12:56:01.490324763 -0800
@@ -33,6 +33,8 @@
 
 	"golang.org/x/net/http/httpguts"
 	"golang.org/x/net/http/httpproxy"
+
+	"runtime/debug"
 )
 
 // DefaultTransport is the default implementation of Transport and is
@@ -1412,6 +1414,8 @@
 func (t *Transport) queueForDial(w *wantConn) {
 	w.beforeDial()
 	if t.MaxConnsPerHost <= 0 {
+		fmt.Println("\n\n\n === queueForDial")
+		debug.PrintStack()
 		go t.dialConnFor(w)
 		return
 	}
@@ -1744,6 +1748,8 @@
 	pconn.br = bufio.NewReaderSize(pconn, t.readBufferSize())
 	pconn.bw = bufio.NewWriterSize(persistConnWriter{pconn}, t.writeBufferSize())
 
+	fmt.Println("\n\n\n === starting http goroutines ===")
+	debug.PrintStack()
 	go pconn.readLoop()
 	go pconn.writeLoop()
 	return pconn, nil
